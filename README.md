Система автоматического развертывания и управления распределенной сетевой инфраструктурой. Решение позволяет разворачивать защищенные узлы связи (VPN-ноды) на "чистых" Linux-серверах за ~2 минуты, включая настройку безопасности, маршрутизации и DNS.

**Цель проекта:** Исключить ручную настройку серверов (Human Error), обеспечить идемпотентность конфигурации и автоматизировать масштабирование инфраструктуры.

![Python](https://img.shields.io/badge/Python-3.11-blue?logo=python)
![Ansible](https://img.shields.io/badge/Ansible-Automation-red?logo=ansible)
![Docker](https://img.shields.io/badge/Docker-Containerization-blue?logo=docker)
![Cloudflare](https://img.shields.io/badge/Cloudflare-DNS_API-orange?logo=cloudflare)

## 🏗 Архитектура решения

Система построена по принципу **Infrastructure as Code (IaC)**.
Управляющий сервис (Python) инициирует процесс деплоя, генерирует инвентарь для Ansible и взаимодействует с внешними API.

1.  **Orchestrator Service:** Python-сервис, принимающий команды на масштабирование.
2.  **Ansible Runner:** Выполняет плейбуки на удаленных хостах через SSH.
3.  **DNS Controller:** Автоматически создает/удаляет A-записи через Cloudflare API.
4.  **Target Nodes:** Серверы Ubuntu, на которых разворачивается Docker-контейнер с ядром маршрутизации (Xray/Marzban) и настраивается окружение.

## 🛠 Ключевые возможности

### 1. Автоматизация настройки сервера (Ansible)
Плейбук `setup_node.yml` выполняет полную настройку "с нуля":
*   Установка зависимостей и Docker.
*   **Security Hardening:** Настройка UFW (разрешены только порты 22, 80, 443), установка Fail2ban.
*   **SSL/TLS:** Автоматическая генерация сертификатов через Certbot (Let's Encrypt).
*   **Deployment:** Запуск ядра в Docker-контейнере с пробросом сертификатов и конфигов.

### 2. Интеграция с Cloudflare
Реализован модуль для работы с DNS-зонами:
*   Динамическое создание поддоменов при добавлении новой ноды.
*   Управление проксированием (Orange cloud vs Grey cloud).
*   Очистка записей при удалении сервера.

### 3. Мониторинг и управление
*   Сбор статистики по трафику с каждой ноды.
*   Группировка нод в кластеры для балансировки нагрузки.
*   Автоматическая проверка доступности и здоровья сервисов.

## 📂 Структура проекта (Key Components)

```text
├── backend/
│   ├── app/
│   │   ├── services/
│   │   │   ├── node_manager.py    # Логика запуска Ansible и Cloudflare API
│   │   │   └── config_generator.py # Генерация клиентских конфигов (JSON/VLESS)
│   ├── ansible/
│   │   └── setup_node.yml         # Основной плейбук настройки сервера
├── docker-compose.yml             # Оркестрация управляющего сервиса
